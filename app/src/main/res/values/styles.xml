<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/primary_material_light</item>
        <item name="colorPrimaryDark">@color/primary_material_dark</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="FragmentLayout">
        <item name="android:paddingLeft">@dimen/space_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/space_horizontal_margin</item>
    </style>

    <style name="icon_big">
        <item name="android:layout_height">@dimen/icon_big</item>
        <item name="android:layout_width">@dimen/icon_big</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <style name="icon_medium">
        <item name="android:layout_height">@dimen/icon_small</item>
        <item name="android:layout_width">@dimen/icon_small</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <style name="FilePickerTheme" parent="Theme.AppCompat.DialogWhenLarge">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryDark">@color/material_text_color_primary_dark</item>
        <item name="colorAccent">@color/brand_primary_accent</item>
        <item name="alertDialogTheme">@style/SampleAlertDialogTheme</item>
        <!-- These are important. Handled by toolbar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="SampleAlertDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryDark">@color/material_text_color_primary_dark</item>
        <item name="colorAccent">@color/brand_primary_accent</item>
    </style>



    <style name="listview_general" parent="FragmentLayout">
        <item name="android:divider">@android:color/transparent</item>
        <item name="android:dividerHeight">10.0sp</item>
    </style>
</resources>
